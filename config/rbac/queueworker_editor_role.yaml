# This rule is not used by the project queueworker-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the apps.mystic-06.github.io.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: queueworker-operator
    app.kubernetes.io/managed-by: kustomize
  name: queueworker-editor-role
rules:
- apiGroups:
  - apps.mystic-06.github.io
  resources:
  - queueworkers
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - apps.mystic-06.github.io
  resources:
  - queueworkers/status
  verbs:
  - get
